# .env.example
#
# Copie ce fichier en .env et remplis les vraies valeurs.
# Ne JAMAIS commiter le fichier .env réel (déjà dans .gitignore).

# --- Local (Docker) — développement quotidien ---
DATABASE_URL="postgresql://uniflow:uniflow_dev_password@localhost:5433/uniflow_dev"

# --- Supabase (staging/prod partagé) — à activer uniquement au déploiement ---
# DATABASE_URL="postgresql://postgres.xxxxx:[password]@aws-0-xxxx.pooler.supabase.com:6543/postgres?pgbouncer=true"
# DIRECT_URL="postgresql://postgres.xxxxx:[password]@aws-0-xxxx.pooler.supabase.com:5432/postgres"